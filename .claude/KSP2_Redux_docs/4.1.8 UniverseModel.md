# UniverseModel

The `UniverseModel` contains everything that the game knows about.

How to get a vessel by name using `UniverseModel`:

```csharp
VesselComponent vesselComponent = GameManager.Instance.Game.UniverseModel
    .GetAllVessels()
    .Where((vessel) => vessel.Name == "Vessel Name Here")
    .First();
```