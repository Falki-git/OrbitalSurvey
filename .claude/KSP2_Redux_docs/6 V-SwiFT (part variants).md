# V-SwiFT (part variants)

:::warning
This documentation is deprecated and will be replaced by a new version for the Lua-based Patch Manager in the near future.

:::

## What it covers

* authoring `Data_PartSwitch` blocks with Patch Manager
* defining `VariantSet` and `Variant` data
* using V-SwiFT transformers for part variants and feature toggles
* understanding the runtime OAB, flight, UI, and persistence behavior that those patches rely on